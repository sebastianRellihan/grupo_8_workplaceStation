<!DOCTYPE html>
<html lang="es">

<%# Import del HEAD del documento %>
<%- include("../partials/head", {usesBootstrap: true, title: "Workplace Station - Detalle del producto",
    description: "¡En Workplace Station te ofrecemos todo lo necesario para armar tu estación de trabajo! ¡Formá parte de nuestra plataforma y tené acceso a todos nuestros productos!",
    keywords: "Workplace Station, detalle del producto, previsualización"})%>

<body>
    <%# Import del HADER %>
    <%- include("../partials/header") %>

    <main class="main-product-detail">
        <% if(product != undefined){ %><%# if para discrminar entre productos existentes e inexistentes %>

            <div class="navmap-container">
                <!-- Mapa de navegación dentro de la sección de productos -->
                <a href="">Inicio</a>
                <span> / </span>
                <a href="">Productos</a>
                <span> / </span>
                <a href=""><%= product.category %></a>
                <span> / </span>
                <a href=""><%= product.name %> </a>
            </div>

            <section class="product-template">
                <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">
                    <ol class="carousel-indicators">
                        <li data-target="#carouselExampleIndicators" data-slide-to="0" class="active"
                            style="background-image: url('<%= product.image %>');"></li>
                        <%# Sólo se muestran las pestañas en caso de que las imágenes estén disponibles %>
                        <% if(product.secondImage != "") { %>
                            <li data-target="#carouselExampleIndicators" data-slide-to="1"
                                style="background-image: url('<%= product.secondImage %>');"></li>
                        <% } %>
                        <% if(product.thirdImage != "") { %>
                            <li data-target="#carouselExampleIndicators" data-slide-to="2"
                                style="background-image: url('<%= product.thirdImage %>');"></li>
                        <% } %>
                    </ol>
                    <div class="carousel-inner">
                        <div class="carousel-item active">
                            <img src="<%= product.image %>" class="d-block w-100" 
                            alt="Imagen ilustrativa del producto">
                        </div>
                        <%# Sólo se muestran las imágenes en caso de que estén disponibles %>
                        <% if(product.secondImage != "") { %>
                            <div class="carousel-item">
                                <img src="<%= product.secondImage %>" class="d-block w-100" 
                                alt="Imagen ilustrativa del producto">
                            </div>
                        <% } %>
                        <% if(product.thirdImage != "") { %>
                            <div class="carousel-item">
                                <img src="<%= product.thirdImage %>" class="d-block w-100" 
                                alt="Imagen ilustrativa del producto">
                            </div>
                        <% } %>
                    </div>
                    <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="sr-only">Previous</span>
                    </a>
                    <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="sr-only">Next</span>
                    </a>
                </div>

                <div class="product-info">
                    <h2><%= product.name %></h2>
                    <p>$<%= product.price %></p>
                    <p><%= product.briefDescription %></p>
                </div>
                <div class="product-more-info">
                    <!-- Bootstrap collapse acordion -->
                    <div id="accordion">
                        <div class="card">
                            <div class="card-header" id="headingOne">
                                <h5 class="mb-0">
                                    <button class="btn btn-link" data-toggle="collapse" data-target="#collapseOne" aria-expanded="true" aria-controls="collapseOne">
                                        Descripción detallada
                                    </button>
                                </h5>
                            </div>
                            <div id="collapseOne" class="collapse" aria-labelledby="headingOne" data-parent="#accordion">
                                <div class="card-body">
                                    <%= product.description %>
                                </div>
                            </div>
                        </div>

                        <div class="card">
                            <div class="card-header" id="headingTwo">
                                <h5 class="mb-0">
                                    <button class="btn btn-link collapsed" data-toggle="collapse" data-target="#collapseTwo" aria-expanded="false" aria-controls="collapseTwo">
                                        Información adicional
                                    </button>
                                </h5>
                            </div>
                            <div id="collapseTwo" class="collapse" aria-labelledby="headingTwo" data-parent="#accordion">
                                <div class="card-body">
                                    <%= product.aditionalInfo %>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="product-actions">
                    <div class="quantity-selector">
                        <button>-</button>
                        <span>1</span>
                        <button>+</button>
                    </div>
                    <button class="action-button">Agregar al carrito</button>
                    <button class="action-button">Comprar ahora</button>
                </div>
            </section>
            <section class="product-recomendation">
            </section>

        <% } else { %>
            <h1>¡UPS! Nada por aquí...</h1>
        <% } %>
    </main>
    
    <%# Import del footer %>
    <%- include("../partials/footer") %>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js" integrity="sha384-DfXdz2htPH0lsSSs5nCTpuj/zy4C+OGpamoFVy38MVBnE+IbbVYUew+OrCXaRkfj" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js" integrity="sha384-Q6E9RHvbIyZFJoft+2mJbHaEWldlvI9IOYy5n3zV9zzTtmI3UksdQRVvoxMfooAo" crossorigin="anonymous"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js" integrity="sha384-OgVRvuATP1z7JjHLkuOU7Xw704+h835Lr+6QL9UvYjZE3Ipu6Tp75j7Bh/kR0JKI" crossorigin="anonymous"></script>
</body>

</html>